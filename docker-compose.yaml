version: '3.8'

services:
  front:
    build: .
    ports:
      - "8088:80"
    depends_on:
      - api

  api:
    image: shashaevkirill/snakesite
    ports:
      - "8000:8000"
    volumes:
      - uploads_data:/app/uploads

volumes:
  uploads_data:
